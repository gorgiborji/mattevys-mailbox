# Supabase Auth Setup (Manual Dashboard Steps)

1. Enable **Email** provider and configure magic link / OTP mode.
2. Enable **Apple** provider (requires Apple developer credentials).
3. Enable **Google** provider (requires Google Cloud OAuth client).
4. Add redirect URL: `mattevy://auth-callback`.
