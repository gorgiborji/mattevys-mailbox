import { SafeAreaView, Text } from 'react-native';

export default function WriteScreen() {
  return (
    <SafeAreaView>
      <Text>Write Screen (port in progress)</Text>
    </SafeAreaView>
  );
}
