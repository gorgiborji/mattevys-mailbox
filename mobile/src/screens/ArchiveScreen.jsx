import { SafeAreaView, Text } from 'react-native';

export default function ArchiveScreen() {
  return (
    <SafeAreaView>
      <Text>Archive Screen (port in progress)</Text>
    </SafeAreaView>
  );
}
